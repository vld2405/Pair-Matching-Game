<Window x:Class="MatchThePairs.Views.AboutWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MatchThePairs.Views"
        mc:Ignorable="d"
        Title="About" Height="150" Width="300"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Window.Resources>
        <Style x:Key="TextBlocks" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#EDE8DC" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="FontFamily" Value="Consolas" />
            <Setter Property="FontSize" Value="16" />

        </Style>
    </Window.Resources>

    <Grid 
        Background="#C9B194">
        <StackPanel 
        HorizontalAlignment="Center">

            <TextBlock Text="Opris Liviu Vlad" Style="{StaticResource TextBlocks}"/>
            <TextBlock Style="{StaticResource TextBlocks}">
            <Hyperlink NavigateUri="mailto:liviu.opris@student.unitbv.ro" 
                       RequestNavigate="Hyperlink_RequestNavigate">
                liviu.opris@student.unitbv.ro
            </Hyperlink>
            </TextBlock>
            <TextBlock Text="10LF332" Style="{StaticResource TextBlocks}"/>
            <TextBlock Text="info aplicata" Style="{StaticResource TextBlocks}"/>
        </StackPanel>
    </Grid>
</Window>
